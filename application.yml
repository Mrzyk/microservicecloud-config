spring:
  profiles:
    active:
    - dev

spring:
  profiles: dev
  application:
    name: microservicecloud-config-zyk-dev

spring:
  profiles: test
  application:
    name: microservicecloud-config-zyk-test